const fallback = (
    <div className="flex justify-center items-center h-screen">
            <p className="text-errMessage font-bold text-4xl">
                Something went wrong, application could not render!
            </p>
    </div>
);

export default fallback;